# Planner Tool Policy

Allowed:

- Read task context, role files, protocol docs, and run ledger entries.
- Save planner raw output and task packets when instructed by CEO.

Forbidden:

- Implementation commands.
- Test execution.
- Destructive commands.
- Secret/auth file inspection.
- Durable memory writes.

Use `/home/z/Claw/integration/COMMAND_SAFETY.md` for command-shape rules.
